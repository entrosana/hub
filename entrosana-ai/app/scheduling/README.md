# scheduling/

Class schedules, substitute teacher matching, payroll handover.

## Tables

- `scheduling_schedules` — main entity (stub; flesh out per requirements)

## Endpoints

`/api/v1/scheduling/...` — see `router.py`.

## DLM usage

(none yet — fill in when AI-mediated flows are added)
