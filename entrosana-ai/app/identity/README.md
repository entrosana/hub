# identity/

Tenants (schools), users, roles, permissions, JWT.

## Tables

- `identity_users` — main entity (stub; flesh out per requirements)

## Endpoints

`/api/v1/identity/...` — see `router.py`.

## DLM usage

(none yet — fill in when AI-mediated flows are added)
